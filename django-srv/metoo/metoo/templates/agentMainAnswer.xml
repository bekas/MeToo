<?xml version="1.0" encoding="UTF-8"?>
<metoo>
	<!--Общие поля для всех пакетов-->
	<type>{{ type }}</type>
	<request_id>{{ request_id }}</request_id> 
	<result>{{ result }}</result>
	<result_mess>{{ result_mess }}</result_mess>
	
	{% if type == "mainType" %}
	<!--Старый тип, может использоваться для отладки-->
		<answer>{{ message }}</answer>
		<agent>{{ agent }}</agent>
		<get>{{ get }}</get>
		<post>{{ post }}</post>
	{% endif %}
	
	{% if type == "auth" %}
    <!--Авторизация.Возвращает id сессии-->
    	<session_id>{{ session_id }}</session_id>
    {% endif %}
    
   	{% if type == "registrate" %}
    <!--Регистрация.Возвращает id сессии-->
    	<session_id>{{ session_id }}</session_id>
    {% endif %}
    
    {% if type == "event_create" %}
    <!--Создание события.Возвращает id события-->
    	<event_id>{{ event_id }}</event_id>
    {% endif %}
    
    {% if type == "events" %}
	<!--Список событий-->
		<events N = "{{ count }}">
		  {% for event in events %}
    	  	<event>
    	  		<id>{{ event.id }}</id>
          		<creator_id>{{ event.creator_id }}</creator_id>
          	    <creator_name>{{ event.creator_name}}</creator_name>
                <name>{{ event.name }}</name>
                <time>{{ event.date }}</time>
                <description>{{ event.description }}</description>
                <photo>{{ event.photo }}</photo>
                <latitude>{{ event.latitude }}</latitude>
                <longitude>{{ event.longitude }}</longitude>
             	<type_id>{{ event.type }}</type_id>      
             	<type_name>{{ event.type_name }}</type_name>          
             </event>
		  {% endfor %}
       </events>
    {% endif %}
    
    
    {% if type == "event_get" %}
	<!--Список событий-->
	  	<event>
	  		<id>{{ event.id }}</id>
      		<creator_id>{{ event.creator_id }}</creator_id>
      	    <creator_name>{{ event.creator_name}}</creator_name>
            <name>{{ event.name }}</name>
            <time>{{ event.date }}</time>
            <description>{{ event.description }}</description>
            <photo>{{ event.photo }}</photo>
            <latitude>{{ event.latitude }}</latitude>
            <longitude>{{ event.longitude }}</longitude>
         	<type_id>{{ event.type }}</type_id>      
         	<type_name>{{ event.type_name }}</type_name>          
         </event>
    {% endif %}
    
    
    {% if type == "users" %}
    <!--Список пользователей-->	
		<users N = "{{ count }}">
		  {% for user in users %}
    	  	<user>  	
    	  		<id>{{ user.id }}</id>
          		<login>{{ user.login }}</login>
          		<avatar>{{ user.avatar }}</avatar>
          		<rating>{{ user.rating }}</rating>
          	</user>
		  {% endfor %}
       </users>
    {% endif %}
    
    {% if type == "friends" %}
    <!--Список друзей-->	
		<users N = "{{ count }}">
		  {% for friend in friends %}
    	  	<user>  	
    	  		<id>{{ friend.id }}</id>
          		<login>{{ friend.login }}</login>
          		<avatar>{{ friend.avatar }}</avatar>
          		<rating>{{ friend.rating }}</rating>
          	</user>
		  {% endfor %}
       </users>
    {% endif %}
</metoo>
